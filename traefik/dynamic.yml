http:
  routers:
    gateway:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web
      service: gateway

  services:
    gateway:
      loadBalancer:
        servers:
          - url: "http://fastapi:8000"
        healthCheck:
          path: /health
          interval: 10s
          timeout: 3s
